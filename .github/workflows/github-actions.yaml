name: bootstrap test

on: [push]
jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Hello
        run: echo ”Hello Github Actions!”

      - name: List
        run: ls -R

      - name: Lint
        run: npm run eslint:check

      - name: Unit tests
        run: npm run mocha:test

      - name: Functionale tests
        run: nyc:run

      